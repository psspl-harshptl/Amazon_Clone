'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up(queryInterface) {
    // Reserved — ProductImages/ProductSpecifications/ProductFeatures tables not yet migrated
  },

  async down(queryInterface) {
    // No-op
  }
};
