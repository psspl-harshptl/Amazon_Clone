'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up(queryInterface) {
    // Reserved for future product-detail sub-tables (specs, variants, etc.)
  },

  async down(queryInterface) {
    // No-op
  }
};
